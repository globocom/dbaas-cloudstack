============
Installation
============

At the command line::

    $ easy_install dbaas-cloudstack

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dbaas-cloudstack
    $ pip install dbaas-cloudstack